/* the data used within this folder is exported from the database in database-admin. refer to database-admin for original source code.
   the only exception is lookups-exercises.xlsx, which uses several datasets made (painfully) by hand. */
